{
  "scripts": {
    "build": "mkdir -p dist/grammar dist/vocab dist/kanji && cp index.html serve.json dist/ && npm run build --prefix grammar-app && cp dist/grammar/index.html dist/vocab/index.html && cp dist/grammar/index.html dist/kanji/index.html && cp dist/grammar/index.html dist/404.html",
    "serve": "npx serve dist"
  },
  "devDependencies": {
    "autoprefixer": "^10.5.4",
    "postcss": "^8.5.26",
    "tailwindcss": "^4.3.3"
  }
}
